# 入会申請をGoogleスプレッドシートで管理する設定手順

`okojin_app.html` の申請フォームは送信時に `GAS_URL`（Google Apps ScriptのウェブアプリURL）へ
データをPOSTする作りになっています。この送信先（`Code.gs`）が、既存の名簿スプレッドシートの
最下部に自動で1行追記するようになっています。

対象スプレッドシート:
https://docs.google.com/spreadsheets/d/1NWLLHauvqE2zRVh31BYsORY48JoqqaTD/edit?gid=2013538450

## すでに動作確認済みのApps Scriptプロジェクトがある場合（このケースに該当）

1. そのApps Scriptプロジェクトを開く
2. 中身をすべて削除し、このフォルダの [`Code.gs`](Code.gs) の内容に置き換えて保存
3. 右上「デプロイ」→「デプロイを管理」→ 既存デプロイの編集（鉛筆アイコン）
4. 「バージョン」を「新バージョン」にして「デプロイ」
   - ウェブアプリのURLは変わらないため、`okojin_app.html` 側の `GAS_URL` は変更不要です
5. スクリプトを実行するアカウントが、対象スプレッドシート（上記URL）への**編集権限**を持っていることを確認してください
   - 権限が無いと書き込み時にエラーになります

## 新規にApps Scriptプロジェクトを作る場合

1. https://script.google.com で新しいプロジェクトを作成（スプレッドシートに紐づける必要はありません。
   `Code.gs` は `openById()` で対象スプレッドシートを直接指定しているため、単体のプロジェクトでも動作します）
2. コードを全て削除し、このフォルダの [`Code.gs`](Code.gs) の内容を貼り付けて保存
3. 右上「デプロイ」→「新しいデプロイ」
   - 種類：ウェブアプリ
   - 実行するユーザー：自分
   - アクセスできるユーザー：全員
   - 「デプロイ」をクリック
   - 初回は「このアプリは確認されていません」という警告が出るので、
     「詳細」→「（プロジェクト名）に移動（安全ではないページ）」→ 許可 と進める
4. 発行された「ウェブアプリのURL」をコピーし、`okojin_app.html` 内の
   ```js
   const GAS_URL = "ここに新しいURLを貼る";
   ```
   を書き換える

## 申請者本人へのLINEグループ案内メールについて

- スプレッドシートへの登録が終わったあと、申請者本人（フォームに入力されたメールアドレス）宛てに、
  大高人グループLINEへの参加案内メールを自動送信します
- メールにはQRコード画像と参加ボタン（リンク）の両方を記載しています
- LINEグループのURLは `Code.gs` 内の `const LINE_GROUP_URL = 'https://line.me/ti/g/HxtSBb8hAe';` です。
  グループURLが変わった場合はここを書き換えてください
- QRコードは外部の無料API（api.qrserver.com）を使ってメール送信のたびに生成しています。
  この外部サービスが利用できない場合でも、QR画像なしでリンクのみのメールとして送信されます

## 通知先メールアドレスについて（任意）

- `Code.gs` 内の `const ADMIN_EMAIL = 'okojin.unei@gmail.com';`
- 新しい申請が来るたびにこのアドレスへ通知メールが届きます（件名は既存の運用形式
  「【大高人】新規入会申請 No.xxx 〇〇 さん」に合わせています）
- 不要な場合は `doPost` 内の `notifyAdmin_(data, nextNo);` の行を削除してください
- 通知先を変えたい場合はこのメールアドレスを書き換えてください

## スプレッドシートに自動入力される項目（既存の名簿の列構成に合わせています）

| 列 | 項目 | 備考 |
|---|---|---|
| A | No | 既存データの最大値+1を自動採番（3桁ゼロ埋め） |
| B | 役割 | 自動入力しない（手動管理） |
| C | 大高人チャット | 自動入力しない（手動管理） |
| D | 大高人通信 | 自動入力しない（手動管理） |
| E | 名前 | フォーム入力値 |
| F | ふりがな | フォーム入力値 |
| G | 卒業年 | フォーム入力値 |
| H | 仕事の内容 | フォーム入力値 |
| I | 現職 | フォーム入力値 |
| J | これまでの経歴 | フォーム入力値 |
| K | 大学 | フォーム入力値 |
| L | 高校時代の主な活動 | フォーム入力値 |
| M | 大学進路を決めた経緯 | フォーム入力値 |
| N | メールアドレス | フォーム入力値 |

## 動作確認

1. `okojin_app.html` をブラウザで開き、テストで1件申請してみる
2. 対象スプレッドシートの最下部に1行追加されるか確認する
3. Apps Scriptエディタの左サイドバー「実行数」で `doPost` が「成功」しているか確認する
   （`doGet` の実行記録は、GAS_URLを直接ブラウザで開いた場合のもので無関係です）

## 補足

- `okojin_app.html` は `mode: "no-cors"` で送信しているため、フォーム側では送信の成功・失敗を
  厳密には判定できません（通信自体が失敗しない限り「完了」画面が表示されます）。実際に届いているかは
  スプレッドシートまたはApps Scriptの「実行数」で確認してください。
- 同時に複数人が送信してもデータが競合しないよう、スクリプト内で排他制御（LockService）を行っています。
- テスト送信したデータが不要な場合は、あとでスプレッドシートから該当行を削除してください
  （「No」列の欠番は特に問題ありません。次回送信時は既存データの最大値+1が振られます）。
