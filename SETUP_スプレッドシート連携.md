# 入会申請をGoogleスプレッドシートで管理する設定手順

`index.html` の申請フォームは送信時に `GAS_URL`（Google Apps ScriptのウェブアプリURL）へ
データをPOSTする作りになっています。現状はその送信先で「スプレッドシートに書き込む」処理が
整っていないため、申請内容を一覧で管理できていません。

このフォルダの `Code.gs` を使ってその処理を用意します。以下の手順で設定してください。

## 手順

1. **申請管理用のGoogleスプレッドシートを用意する**
   - 新規にスプレッドシートを作成する（例：「大高人 入会申請管理」）
   - すでに管理用に用意しているシートがあれば、それを使ってもOK

2. **Apps Scriptエディタを開く**
   - スプレッドシートのメニュー「拡張機能」→「Apps Script」

3. **コードを貼り付ける**
   - 開いたエディタの中身（`function myFunction() {}` など）を全て削除
   - このフォルダの [`Code.gs`](Code.gs) の内容をすべて貼り付ける
   - 上部の保存アイコン（💾）で保存

4. **通知先メールアドレスを確認する（任意）**
   - `Code.gs` 内の `const ADMIN_EMAIL = 'okojin.unei@gmail.com';` の部分
   - 新しい申請が来るたびにこのアドレスへ通知メールが届く設定になっています
   - 不要な場合は `doPost` 内の `notifyAdmin_(data);` の行を削除してください
   - 通知先を変えたい場合はこのメールアドレスを書き換えてください

5. **ウェブアプリとしてデプロイする**
   - 右上「デプロイ」→「新しいデプロイ」
   - 歯車アイコン →「ウェブアプリ」を選択
   - 「実行するユーザー」：自分
   - 「アクセスできるユーザー」：全員
   - 「デプロイ」をクリック
   - 初回は「このアプリは確認されていません」という警告が出るので、
     「詳細」→「（プロジェクト名）に移動（安全ではないページ）」→ 許可 と進める
     （これは自分で作成したスクリプトなので問題ありません）

6. **発行されたURLを控える**
   - デプロイ完了画面に表示される「ウェブアプリ」のURL（`https://script.google.com/macros/s/.../exec` の形式）をコピー

7. **`index.html` のURLを更新する**
   - `index.html` 内の以下の行を、6で控えたURLに書き換える
     ```js
     const GAS_URL = "ここに新しいURLを貼る";
     ```
   - 現在入っているURL（`.../AKfycbzIQpnD83VoWbJP9s2MtPeFdytBVSRxRbdgLOttqEHhM1gcmUFP3WVbFu1nHwEheZwgxg/exec`）に
     心当たりのあるApps Scriptプロジェクトがある場合は、そのプロジェクトのコードを `Code.gs` の内容に置き換えて
     「デプロイを管理」から既存デプロイを編集・再デプロイすれば、URLを変えずにそのまま使えます

8. **動作確認**
   - `index.html` をブラウザで開き、テストで1件申請してみる
   - スプレッドシートに「入会申請一覧」というシートが自動で作られ、そこに1行追加されればOK

## スプレッドシートに記録される項目

受付日時、名前、ふりがな、卒業年、メールアドレス、現職、仕事の内容、これまでの経歴、大学、
高校時代の主な活動、大学進路を決めた経緯

## 補足

- `mode: "no-cors"` で送信しているため、フォーム側では送信の成功・失敗を厳密には判定できません
  （通信自体が失敗しない限り「完了」画面が表示されます）。実際に届いているかはスプレッドシートで確認してください。
- 同時に複数人が送信してもデータが競合しないよう、スクリプト内で排他制御（LockService）を行っています。
